# see: https://docs.floydhub.com/floyd_config
# All supported configs:
#
env: tensorflow-1.9
input:
  - destination: weights
    source: aoboturov/datasets/cpm_hand/1
#description: this is a test
#max_runtime: 3600

# You can also define multiple tasks to use with --task argument:
#
task:
  serve:
    machine: cpu
    mode: serve
